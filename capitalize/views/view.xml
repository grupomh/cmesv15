<odoo>
    <data>
    <record id="capitalize_name_contact" model="ir.ui.view">
        <field name="name">capitalize.name.contact</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
                    <field name="mobile" position="after">
                        <field name="capitalize_name_contact" invisible="1"/>
                        <field name="capitalize_street_contact" invisible="1"/>
                    </field>
                </field>
            </record>
            </data>
    </odoo>