<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data>
        <!-- stock.move inherit search view -->
        <record id="view_move_search_extend" model="ir.ui.view">
            <field name="name">view.move.search.extend</field>
            <field name="model">stock.move</field>
            <field name="inherit_id" ref="stock.view_move_search"/>
            <field name="arch" type="xml">
                <filter name="by_product" position="after">
                    <filter string="Routes" name="by_routes" domain="[]" context="{'group_by': 'route'}"/>
                </filter>
            </field>
        </record>

        <!-- stock.picking inherit search view -->
        <record id="view_picking_internal_search_extend" model="ir.ui.view">
            <field name="name">view.picking.internal.search.extend</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_internal_search"/>
            <field name="arch" type="xml">
                <filter name="status" position="after">
                    <filter string="Routes" name="by_routes" domain="[]" context="{'group_by': 'route'}"/>
                </filter>
            </field>
        </record>
    </data>
</odoo>