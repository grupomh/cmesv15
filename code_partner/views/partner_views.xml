<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data>
        <!-- model.name inherit form view -->
        <record id="view_partner_form_extend" model="ir.ui.view">
            <field name="name">view.partner.form.extend</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <field name="vat" position="after">
                    <field name="route"/>
                    <field name="number" invisible="1"/>
                    <field name="code"/>
                </field>
            </field>
        </record>

        <record id="view_res_partner_inherit_search" model="ir.ui.view">
            <field name="name">res.partner.search.expand.filter</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_res_partner_filter"/>
            <field name="arch" type="xml">
                <field name="name" position="after">
                    <field name="code" string="Codigo cliente"/>
                    <field name="vat" string="Nit cliente"/>
                </field>
            </field>
        </record>

    </data>
</odoo>